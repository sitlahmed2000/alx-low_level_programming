#include "main.h"

/**
 * _abs - checks for alphabetic character
 * @n: the character to be checked
 * Return: the corrent answer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
